/**
 * tenant.js — Campanha Betão (tenant 2)
 *
 * TENANT_ID      → slug de texto usado na tabela tenant_config
 * TENANT_NUM_ID  → id numérico da tabela usuarios
 */
window.TENANT_ID     = '2';
window.TENANT_NUM_ID = 2;
